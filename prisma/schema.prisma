// This is your Prisma schema file
// Learn more: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Game {
  id              String    @id @default(cuid())
  sgf             String    @unique
  playerColor     String    // "white" or "black"
  opponentRank    String?   // Go rank (e.g., "5k", "2d")
  timeControl     String?
  datePlayed      DateTime?
  createdAt       DateTime  @default(now())

  mistakes        Mistake[]

  @@index([datePlayed])
}

model Mistake {
  id                 String   @id @default(cuid())
  gameId             String
  game               Game     @relation(fields: [gameId], references: [id], onDelete: Cascade)
  moveIndex          Int      // Exact position in moves array (0 = start, 1 = after first move, etc.)
  boardState         String   // JSON serialized BoardState (signMap, boardSize, captures)
  briefDescription   String
  primaryTag         String
  detailedReflection String?
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@index([gameId])
  @@index([primaryTag])
  @@index([createdAt])
}

model Insight {
  id               String   @id @default(cuid())
  content          String   // JSON array of insight objects
  mistakesAnalyzed Int
  mistakeIdsMap    String?  // JSON array of mistake IDs in order (for reference lookups)
  createdAt        DateTime @default(now())

  @@index([createdAt])
}
